import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primaryColor = Color(0xFFA90140);
  static const Color secondaryColor = Color(0xFF550120);
  static const Color backgroundColor = Color(0xFFFFFFFF); // White
  static const Color darkColor = Color(0xFF1A1A1A); // Dark Gray (Better contrast)

  // 📝 Text Colors
  static const Color textColor = Color(0xFF1A1A1A); // Dark Gray (Better contrast)
  static const Color lightTextColor = Color(0xFFFFFFFF); // White
  static const Color errorColor = Color(0xFFD32F2F); // Red for errors

  // 🎛️ Button & UI Colors
  static const Color buttonColor = primaryColor;
  static const Color buttonTextColor = Colors.white;
  static const Color shadowColor = Color(0x33000000); // Black with transparency
  static const Color dividerColor = Color(0xFFBDBDBD); // Neutral gray

  // ✅ Status Colors
  static const Color successColor = Color(0xFF4CAF50); // Green for success
  static const Color warningColor = Color(0xFFFFC107); // Yellow for warnings

// ✅ New App Icon Color
  static const Color appIconColor = Color(0xFFFFFFFF); // Adjust the color as needed
}
